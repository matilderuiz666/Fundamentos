#include <iostream>

using namespace std;

int main()
{
	int n;
	do {
		cout << "ingrese la tabla de multiplicar que desea del 1 al 10: ";
		cin >> n;
	} while (n < 0 || n>10);

	for (int i = 1; i <= 10; i++) {
		cout << i << " x " << n << " = " << i * n << endl;
	}
}
